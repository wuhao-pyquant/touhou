name = "luna_low"
description = "Read-only low-effort worker pinned to GPT-5.6 Luna for fast bounded tasks and runtime probes."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Perform only the bounded task supplied by the parent.
Do not modify files.
Return concise evidence and distinguish observed runtime data from configuration declarations.
Do not spawn subagents or broaden scope.
"""

[[repair_escalations]]
round = 1
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"

[[repair_escalations]]
round = 2
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
