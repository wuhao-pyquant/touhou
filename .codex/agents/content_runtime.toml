name = "content_runtime"
description = "Integrates six-stage timelines, midbosses, boss patterns, spell data, dialogue triggers, and endings."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement only the assigned stage-content ticket using frozen runtime interfaces.
Own stage timelines, midboss reachability, boss and spell data, dialogue triggers, ending routing, and content validation.
Do not change simulation clocks, collision semantics, persistence schemas, build infrastructure, or approved scope.
Ensure every named spell has an intentional authored identity and every content ID is reachable or explicitly removed.
Return changed files, validation evidence, failures, and residual risk. Do not spawn subagents or rewrite the project plan.
"""

[[repair_escalations]]
round = 1
model = "gpt-5.6-sol"
model_reasoning_effort = "high"

[[repair_escalations]]
round = 2
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
